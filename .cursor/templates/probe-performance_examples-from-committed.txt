# Re-validate performance_examples maxima: baseline = current CI -loops in
# performance_examples-perf.alias-test; cap raised to see if anything higher still passes.
# Run from repo root (omit --preset so this file is the only dimension source):
#   python3 .cursor/scripts/probe_perf_boundaries.py \
#     --alias-dir src/gvars/utils/performance_examples \
#     --stress-alias performance_examples-perf \
#     --dimensions-file .cursor/templates/probe-performance_examples-from-committed.txt \
#     --max-binary 20 --timeout 120
#
# After editing performance_examples-perf.alias-test -loops, sync the baseline numbers below.

benchmark adv dice list index loop should keep checksum,loops,4280,120000
benchmark adv dice if chain loop should keep checksum,loops,3380,120000
benchmark three way list index loop should keep checksum,loops,4450,120000
benchmark three way if chain loop should keep checksum,loops,3920,120000
benchmark dict get loop should keep checksum,loops,6550,120000
benchmark dict in and subscript loop should keep checksum,loops,5750,120000
benchmark tuple membership loop should keep checksum,loops,4450,120000
benchmark list membership loop should keep checksum,loops,4450,120000
benchmark dict bracket known key loop should keep checksum,loops,7580,120000
benchmark dict get known key loop should keep checksum,loops,6550,120000
benchmark counter plus assign loop should keep checksum,loops,9800,120000
benchmark counter plus eq loop should keep checksum,loops,9800,120000
benchmark string concat assign loop should keep checksum,loops,8930,120000
benchmark string concat plus eq loop should keep checksum,loops,9850,120000
